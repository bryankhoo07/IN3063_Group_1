GPU acceleration is recommended but not necessary

Create a function to check if system is CUDA compatible, if so then use CUDA.

Might be possible to use whatever AMD, INTEL or Mac uses, but it might not be possible

Using PyTorch 2.5.1

AMD: PyTorch ROCm - Not available on Windows - only Linux

Intel: xpu - Beta in PyTorch 2.5

Mac: No

